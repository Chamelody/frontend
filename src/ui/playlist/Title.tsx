import React from "react";


const Title = (): JSX.Element => {
    return (
        <div
            style={{
                backgroundColor: 'orange',
                height: "100%"
            }}
        >
            Title
        </div>

    );
};

export default Title;
